//Constantes, valores que una vez declarados no se puede cambiar su valor, por buenas practicas van en mayusculas y se separan las palabras por un guion bajo.
const MI_CONSTANTE = 3.1416;

//MI_CONSTANTE = 25; No podemos cambiar el valor a una constante
console.log(MI_CONSTANTE);

//Existe constantes ya definidas en el lenguaje de JS como PI
console.log(Math.PI);

//Constante de segundos por minuto
const SEC_PER_MIN = 60;
console.log(SEC_PER_MIN);